<%= form_for([:admin, @prototype, @page, @page_version]) do |f| %>
  <%= f.error_messages %>

  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
